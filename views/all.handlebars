{{! <div>Tech Blog</div> }}

{{#each posts as |post|}}
  <div class="dish col-md-5">
    <a href="/post/{{post.id}}">
    <p data-id="{{id}}" class="card-header">
      {{post.title}}
      {{format_date post.createdAt}}
    </p>
    </a>
  </div>
{{/each}}